<<% theme_flashcards_to_learn = theme_flashcards_to_learn(@theme).count %>
<% theme_flashcards_to_revise = theme_flashcards_to_revise(@theme).count %>

  div class="theme-title theme-show">
  <h1><i class="fas fa-atlas"></i><%=@theme.name.upcase%></h1>
</div>

<div class = "flashcards-results">
  <div class="my-5"></div>
  <div class = "flashcard-achievement">
    <div class="end-flashcards-sentence">
      <p> Félicitations !! <br> <strong> <%= @flashcard_dealt_with_count %> flashcards <%= "revue".pluralize(@flashcard_dealt_with_count)%> !! </strong></p>
    </div>
  </div>
  <div class="container">
    <div class="row">
      <%= render "flashcards_progress_card" %>
      <%= render "statistics_card" %>
    </div>
  </div>
  <div class="my-5"></div>
    <div class="d-flex justify-content-center">
      <%= link_to "MENU", themes_path, class: "btn btn-purple my-4 mx-2" %>
      <% if theme_flashcards_to_learn > 0%>
        <%= link_to "APPRENDRE <br/> (#{theme_flashcards_to_learn})".html_safe, "/theme/#{@theme.id}/flashcards" + "/learn",class: "btn btn-green my-4 mx-2" %>
      <% elsif theme_flashcards_to_revise > 0%>
       <%= link_to "REVISER <br/>(#{theme_flashcards_to_revise})".html_safe, "/theme/#{@theme.id}/flashcards" + "/revise",class: "btn btn-green my-4 mx-2" %>
      <% end %>
    </div>
  <%= render "shared/footer" %>
</div>
