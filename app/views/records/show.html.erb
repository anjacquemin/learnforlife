<div data-controller="record" data-total-unlock-items = <%=@total_unlocked_items%>>
  <%= render 'shared/title_banner', quizz: @record.quizz, quizz_level: @record.quizz_level %>
  <%= render "achievements_calculation" %>
  <div class="my-5"></div>
  <%= render "key_results" %>
  <div class="container">
    <div class="row">
      <%= render "progress_card" %>
      <%= render "answers_card" %>
    </div>
    <div class="row">
      <%= render "leaderboard_card" %>
      <%= render "challenge_card" %>
    </div>

    <div class="my-5"></div>

    <div class="d-flex justify-content-center">
      <%= link_to "MENU", themes_path, class: "btn btn-purple my-4 mx-2" %>
      <%= link_to "QUIZZS", subtheme_path(@record.quizz_level.category.subtheme), class: "btn btn-green my-4 mx-2" %>
    </div>

    <div class="my-5"></div>



  </div>
</div>

  <% sentence = "
    <h2>Gagne de l'expérience en remportant de nouvelles couronnes : </h2>
    <br>
    <p>- 1 couronne : 90% de bonnes réponses</p>
    <p>- 2 couronnes : 100% de bonnes réponses</p>
    <p>- 3 couronnes: 100% de bonnes réponses en un certain temps</p>
    "
    %>
  <%= render "shared/user_indications", sentence: sentence %>

<%= render "shared/footer" %>
<%# TEST MODAL DESIGN %>
